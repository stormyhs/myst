import io;

fn add(left, right) {
    return left + right;

    println("this shoudl never ruin");
}

fn subtract(left, right) {
    return left - right;
}

fn main() {
    fn doThing() {
        let result = add(1, 2) + subtract(3, 4);
    }

    return doThing();
}
