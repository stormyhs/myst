func sub(a, b) {
    let result = a - b;

    result;
}

func add(a, b) {

    func dostuff(num) {
        a + 5 - 5;
    }

    a = dostuff(a);

    let result = a + b;

    result;
}

let a = add(2, 3);
let b = sub(10, 5);

a + b;
